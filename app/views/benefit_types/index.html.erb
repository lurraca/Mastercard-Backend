<h1>Listado de tipos de benficios</h1>

<table class="table">
  <tr>
    <th>Nombre</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @benefit_types.each do |benefit_type| %>
  <tr>
    <td><%= benefit_type.name %></td>
    <td><%= link_to 'Ver', benefit_type %></td>
    <td><%= link_to 'Editar', edit_benefit_type_path(benefit_type) %></td>
    <td><%= link_to 'Eliminar', benefit_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Crear tipo de beneficio', new_benefit_type_path %>
