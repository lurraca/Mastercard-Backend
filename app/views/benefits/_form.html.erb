<%= form_for(@benefit) do |f| %>
  <% if @benefit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@benefit.errors.count, "error") %> prohibited this benefit from being saved:</h2>

      <ul>
      <% @benefit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Nombre" %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description, "Descripción" %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :begin_date, "Fecha de inicio" %><br />
    <%= f.date_select :begin_date %>
  </div>
  <div class="field">
    <%= f.label :end_date, "Fecha Final" %><br />
    <%= f.date_select :end_date %>
  </div>
  <div class="field">
    <%= f.label :business_id, "Negocio" %><br />
	<%= f.select :business_id, @business, {:include_blank => true} %>
  </div>
  <div class="field">
    <%= f.label :benefit_type_id, "Tipo de beneficio" %><br />
	<%= f.select :benefit_type_id, @benefit_types, {:include_blank => true} %>
  </div>
  <div class="actions">
	  <%= f.submit "Guardar", :class=>'btn btn-inverse' %>
	  <%= link_to 'Cancelar', benefits_path, :class=>'btn btn-danger' %>
  </div>
<% end %>
